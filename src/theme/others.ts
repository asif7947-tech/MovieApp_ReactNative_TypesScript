import {moderateScale} from './responsive';
import {T_RADIUS} from './types';

export const borderRadius: T_RADIUS = {
  4: moderateScale(4),
  8: moderateScale(8),
  12: moderateScale(12),
  16: moderateScale(16),
  20: moderateScale(20),
  24: moderateScale(24),
};
